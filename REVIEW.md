# Полное ревью кода `index.html` — SNP Optimizer Insight

**Дата:** 2026-09-04
**Объект:** одностраничное приложение (SPA) для анализа лога `optimizer_status` планировщика SNP.
**Стек:** чистый HTML + CSS + JS, SheetJS (XLSX) через CDN, Canvas для графиков.

---

## 1. Итоговая оценка

Приложение функционально продумано: есть обзор, разрез по сущностям, ограничениям, штрафам,
истории, сырым данным и автоматическим проверкам качества. Логика бизнес-выводов хорошая.

Но при первом запуске на реальных данных два ключевых раздела — **«Ограничения и узкие
места»** и **«Штрафы (SOFT)»** — оказались **полностью неработающими** из-за ошибки в
регулярных выражениях. Это главный баг, который вёл пользователя к неверному выводу
«данных нет», хотя данные в файле есть.

Итог: **критический баг исправлен**, добавлены понятные уведомления вместо браузерных
`alert`, drag-and-drop загрузка, защита от недоступного `localStorage`, базовая
доступность и ряд мелких улучшений.

---

## 2. Критические баги (исправлены)

### 2.1. Парсер границ и штрафов не распознавал данные

В `buildRun()` регулярные выражения для «Lower/Upper bound reached» и «Softmin/Softmax
penalties» содержали лишние символы:

```js
// Было (не работает):
/^Lower bound reached (\w+):(\w+) $([A-Z]+)$ %$/   // лишний пробел и $ перед пробелом
/^Softmin penalties sum (\w+):(\w+) $([A-Z]+)$$/    // нет экранированной скобки

// Стало (работает):
/^Lower bound reached (\w+):(\w+) \(([A-Z]+)\) %$/
/^Softmin penalties sum (\w+):(\w+) \(([A-Z]+)\)$/
```

**Доказательство.** Скрипт с фактическими строками демо-набора:

```
Ни один из паттернов не совпал.
После исправления: bounds = 6, penalties = 1 (production:aggregated_production).
```

**Последствие до фикса:** вкладки «Ограничения и узкие места» и «Штрафы (SOFT)»
всегда показывали:
- «Нет данных по границам»
- «Мягких нарушений не обнаружено»

Хотя данные в файле были. Пользователь получал **ложный положительный вывод «всё
хорошо»** и не видел ключевые риски (узкие места мощности, мягкие нарушения).

Это исправлено: `bounds` и `penalties` теперь парсятся из всех стандартных строк.

### 2.2. Ошибочный парсинг синего канала в `onFill()`

```js
// Было: b = parseInt(h.slice(4,6),16) — считывал перекрывающийся сегмент.
// Стало: b = parseInt(h.slice(5,7),16) — корректный синий канал.
```

Из-за этого контраст текста внутри секторов пончатов рассчитывался неверно (например,
`#1ba84c` давал синий `132` вместо `76`), поэтому цвет подписи мог быть нечитаемым на
некоторых цветах.

### 2.3. Выпадение сущностей, у которых есть только `%` использования

Фильтр `total>0 || value!==0` отбрасывал блок, если в логе есть только строка
`Non-zero values ... %`, но нет строки `Non-zero values ...` с «N / M». Добавлено
условие `|| pct>0`.

---

## 3. Ошибки UX / «пользовательских» багов (исправлены)

### 3.1. `alert()` при ошибках загрузки

Браузерные `alert` блокируют интерфейс и плохо выглядят. Заменены на ненавязчивый
toast-баннер + понятный статус в шапке.

### 3.2. Неинформативное сообщение о количестве добавленных прогонов

Раньше писалось «Загружено: N прогон(ов)» даже если все дубликаты и ничего нового не
добавилось. Теперь показывается:
`Добавлено: X из Y` и отдельно `повторных (уже в истории): Z`.

### 3.3. Молчаливый fallback на «первый лист» файла

Если листа `optimizer_status` нет, приложение раньше просто читало первый лист без
предупреждения. Теперь пользователь видит:
`лист optimizer_status не найден — прочитан первый лист`.

### 3.4. Утечка `Blob`-URL при экспорте CSV

`URL.createObjectURL()` раньше не освобождался. Добавлен `URL.revokeObjectURL()` и
надёжная вставка ссылки в DOM для Safari.

### 3.5. Крах при недоступном `localStorage`

В приватном режиме / при отключённых cookies вызов `localStorage` в самом начале мог
бросить исключение и **уронить весь скрипт**. Обёрнуто в `lsGet`/`lsSet` с try/catch.

---

## 4. Улучшения для конечного пользователя (добавлены)

| Улучшение | Что даёт |
|---|---|
| **Drag & drop загрузки файлов** | Перетащите `.xlsx` прямо на страницу — не нужно искать кнопку. Сверху появляется подсказка с рамкой. |
| **Toast-уведомления** | Понятные сообщения «Загружено / ошибка / CSV сохранён / история очищена» вместо `alert`. |
| **Точная информация о дубликатах** | Пользователь понимает, сколько прогонов реально добавилось. |
| **Предупреждение о отсутствии листа** | Не даёт молча читать «не тот» лист. |
| **Защита от недоступного localStorage** | Приложение запускается даже в приватном режиме. |
| **Базовые ARIA-атрибуты** | `aria-label` у кнопок, `aria-pressed` у переключателя темы, `aria-current` у активного пункта меню, `aria-live` у статуса. |
| **Фокус-стили + тултипы по фокусу** | Удобнее работать с клавиатуры. |
| **`prefers-reduced-motion`** | Уважает системную настройку «уменьшить анимацию». |

---

## 5. Бэклог и статус (после §8)

| # | Пункт | Статус |
|---|---|---|
| 1 | Тесты парсера `parseOptimizerRows` | ✅ `tests/parser.test.js` (55 assertions) |
| 2 | Разбить монолит (Vite/модули) | ⏳ не сделано — zero-build SPA намеренно |
| 3 | ARIA для графиков и focus-trap модалок | ✅ canvas `role=img`, dialog trap, Escape |
| 4 | Мета загрузки + отчёт + печать | ✅ runbar meta, HTML-отчёт, `@media print` |
| 5 | Персистентность истории (IndexedDB) | ✅ автосохранение + «Сохранить сессию» |
| 6 | `dtable`-фильтры без потери фокуса | ✅ инкрементальный `.dtp-l` + restore focus |
| 7 | CSV-разделитель `;` / `,` / авто | ✅ селект в таблице, localStorage |

Подробности реализации — в **§8**.

---

## 6. Что проверено после правок

- `node --check` по извлечённому `<script>` — синтаксис корректен.
- Прогнан изолированный тест парсера на демо-строках — `bounds = 6`, `penalties = 1`,
  `entities = 5`.
- Смоук-тест всего скрипта на минимальном DOM-шимме — скрипт инициализируется без
  ошибок; все вкладки (`ov`, `ent`, `bnd`, `pen`, `raw`, `dq`) отрисовываются без
  исключений.
- Пересмотрены `git diff` — изменения локальны и не затрагивают структуру HTML/CSS.

---

## 7. Итог

**Было:** два ключевых аналитических раздела не работали на реальных данных,
пользователь не получал предупреждений о рисках, а интерфейс в ошибках полагался на
`alert`.

**Стало:** критический парсинг исправлен, добавлены понятные уведомления и
drag-and-drop, улучшены доступность и устойчивость к приватным режимам браузера.
Приложение готово к использованию на реальных выгрузках `optimizer_status`.

---

## 8. Доработки по §5 (сессия arena/01a06c84-opti)

Реализованы пункты из раздела 5 — без разбиения монолита на сборщик (это отдельный
крупный рефакторинг). Приложение по-прежнему один `index.html`, но покрыто тестами
и получило персистентность, отчёты и улучшения a11y/CSV/фильтров.

### 8.1. Тесты парсера

- Файл: `tests/parser.test.js`
- Запуск: `node tests/parser.test.js` (или `npm test`)
- 10 сценариев / **55 assertions**, все green:
  1. Демо-seed: entities=5, bounds=6, penalties≥1, meta/config
  2. Английские имена столбцов (`RunId`/`Parameter`/`Value`)
  3. Группировка нескольких прогонов
  4. Сущность только с `%` (без `N / M`) — не отбрасывается
  5. `parseCalcTime`
  6. Нулевые SOFT-штрафы фильтруются
  7. Регрессия: «сломанный» regex границ не матчит, исправленный — матчит
  8. Синий канал `onFill` (`#1ba84c` → 76)
  9. CSV-разделитель и экранирование
  10. `runKey` / `num`

### 8.2. IndexedDB — история переживает перезагрузку

- Store `snp_opt_insight` / `session` / ключ `current`
- Автосохранение после загрузки XLSX, смены прогона/вкладки и выбора в «Истории»
- Кнопка **«Сохранить сессию»** в runbar (для явного сохранения)
- При старте `boot()` восстанавливает сессию; иначе — демо-набор
- «Очистить» сбрасывает и IndexedDB, и UI

### 8.3. Метаданные загрузки + отчёт + печать

- В runbar: источник (имя файла / демо / сессия), время загрузки, примечание по листу,
  отметка о сохранении
- Кнопка **отчёта** (📄) — HTML-файл с KPI, сущностями, границами, штрафами
- Кнопка **печати** — `@media print` прячет rail/actions, оставляет контент

### 8.4. Focus-trap и ARIA у фильтров / графиков

- Попап `#dtpop`: `role="dialog"`, `aria-modal`, focus-trap (Tab/Shift+Tab), Escape,
  возврат фокуса на кнопку-якорь
- Чекбоксы фильтра **не теряют фокус** при перерисовке: обновляется только
  `.dtp-l`, таблица — отдельно, фокус восстанавливается на тот же `data-v`
- Canvas: `role="img"` + `aria-label` из заголовка карточки
- То же focus-trap на мультивыбор прогонов в «Истории»

### 8.5. Разделитель CSV

- В каждой таблице с экспортом — селект **`;` (RU) / `,` (US) / авто**
- Выбор пишется в `localStorage` (`snp_opt_csv_sep`)
- Корректное CSV-экранирование кавычек и самого разделителя; BOM UTF-8 сохранён

### 8.6. Что сознательно не сделано

- **Разбиение монолита на Vite-модули** — отдельный объём работ; текущий SPA
  остаётся zero-build для простоты развёртывания одним файлом.
- Fixture-XLSX бинарники не коммитятся — seed-строки в коде + unit-тесты покрывают
  те же паттерны, что и реальный лист.

### 8.7. Проверка

```
node tests/parser.test.js          → 55 passed, 0 failed
node --check (извлечённый script)  → OK
smoke tabs ov/ent/bnd/pen/hist/raw/dq + buildReportHtml → OK
```
