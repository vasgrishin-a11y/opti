{
  "name": "snp-optimizer-insight",
  "private": true,
  "description": "SNP Optimizer Insight — single-page analyzer for optimizer_status logs",
  "scripts": {
    "test": "node tests/parser.test.js",
    "check": "node -e \"const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const m=h.match(/<script>([\\\\s\\\\S]*)<\\\\/script>/);fs.writeFileSync('/tmp/opti-check.js',m[1]);\" && node --check /tmp/opti-check.js"
  }
}
