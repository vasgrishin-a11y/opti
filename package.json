{
  "name": "snp-optimizer-insight",
  "private": true,
  "description": "SNP Optimizer Insight — analyzer for optimizer_status logs (XLSX + PostgreSQL)",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node tests/parser.test.js && node tests/server.test.js && node tests/pg-ui.test.js",
    "check": "node -e \"const fs=require('fs');const h=fs.readFileSync('index.html','utf8');const m=h.match(/<script>([\\s\\S]*)<\\/script>/);fs.writeFileSync('/tmp/opti-check.js',m[1]);\" && node --check /tmp/opti-check.js && node --check server.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "pg": "^8.23.0"
  },
  "devDependencies": {
    "jsdom": "^30.0.1"
  }
}
